/* const validateLogin = (user) => {
  const { email, password } = user;
  if (!email || !password) {
    return { message: 'Some required fields are missing' };
  }
};

module.exports = {
  validateLogin,
};
 */